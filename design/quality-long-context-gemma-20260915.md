# LC-G1: conditional Gemma31 whole-evidence candidate

Declared before registration. Activate only after SS1 has a completed, restored primary score below4, with its exact local draft and genuine scalar replayed. If SS1 reaches4, defer this experiment and validate SS1 first. This candidate tests an unmeasured deployment/task combination: the installed Gemma4-31B QAT model reads the entire immutable B1 compact raw-evidence request directly, without a Qwen Japanese intermediate. It changes neither B1 instruction/body/schema nor the evidence pool and adds no ASR, interpretation, hint, reference or local semantic checker.

Use `profiles/long-context-gemma.json`: the same pinned Gemma31 weights/binary and selected temperature1.0/top_p.95/top_k64/repeat_penalty1.0/seed20260913, thinking off, context163840, output reserve16384, full_swaFalse, one sequence, GPU layers99, CPU-MoE0, threads4. Existing owned-backend launch sets Q8 K/V, flash attention, batch512/microbatch128 and fit off. Native prompt admission must use the actual complete apply-template/tokenize result and require prompt+16384+64<=163840. The memory audit establishes plausibility, not successful allocation or quality. Allocation/admission failure ends the configuration without truncation, context escalation or a replacement request.

One candidate and exactly one native stream are permitted, with no retry or hidden transport fallback. The per-call ceiling is the remaining1980-second work allowance; the complete local campaign has2400 seconds, reserving420 for cleanup. Charge prerequisite replay, asset verification, preparation, model transition, capacity, generation, exact-output validation and scalar-receipt validation. Cleanup always closes/stops/restores the owned backend, including after time exhaustion; charge its actual duration and mark any overrun failed afterward. Interrupted streams whose partial capture is unavailable remain terminal and are recorded honestly.

Retain all66 returned Chinese owner strings exactly and apply only the original owner IDs/timestamps. Empty, missing, duplicate or extra keys, malformed JSON, non-normal stop, reasoning output, missing native token usage, incomplete output or a failed strict SRT roundtrip stop the candidate. Do not normalize whitespace, filter owner changes, roll back selected text, request an improved draft or attach previous Chinese to the writer. The exact original Japanese, context and799-observation pool remain the independent review inputs. No generated intermediate or local diagnosis becomes evidence authority.

The CLI separates preparation, local generation and scalar review. One designated primary may be dispatched; exact-target same-pool completed primaries are reused, while incomplete/failed reservations block rerolls. Registries include SS1, R1, A1, B1, RT1, LQ1 and both full-draft diagnostics, plus the existing native review catalog. A passing primary may receive only one designated confirmation on identical inputs. Any prior confirmation for the exact target must be reused or block a fresh attempt if incomplete; a failed confirmation cannot be rerolled. Both scores>=4 establish the contextual benchmark milestone only. The owner-level draft is not automatically the selected/default output or a finished display/release artifact, and acoustic truth remains unverified.

Implementation is in new `scripts/long_context_gemma_screen.py` and `src/long_context_gemma_native.py`, reusing frozen direct-input/review helpers and unconditional cleanup accounting. No predecessor producer is edited. Preparation authenticates B1 directly and binds terminal SS1 native output/score artifacts; it does not run historical per-owner semantic gates or replay every old model deployment. Preserve all failures, native receipts and timing. Stop after the one candidate/primary/conditional confirmation or terminal failure; further changes require a distinct declared experiment.
