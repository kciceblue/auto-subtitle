# Corrected editorial request

Both prior Qwen editors returned every Chinese owner unchanged. The diagnosed
variant's local Gemma critic had found two validly quoted wording problems.
Inspection identified conflicting inherited instructions: the editor was asked
to improve phrasing but also told `其他字符和空白原样保留`, alongside citation-only
language about not rewriting. That is a plausible explanation for copying, not
a demonstrated causal result.

This separately versioned request explicitly permits changing Chinese words,
phrasing, punctuation and order while retaining every meaning and ambiguity.
It uses ordinary owner records rather than the evidence-table instructions.
The complete frozen pilot remains the input; all owners receive the same
instruction. The only editorial findings are those already generated by the
local Chinese critic. Authenticate and reuse its exact native request and
response; do not rerun the critic or add external suggestions.

Everything after the single Qwen edit is unchanged: existing sampler and native
receipt checks, eight unconsumed development controls (all four errors rejected,
at least three equivalent edits accepted), changed-window Gemma source/before/
after checks, monotone rollback and only necessary final-context rechecks. No
downloads or baseline/source reruns. An unchanged result receives no new review.

If changed, apply the common exact-text formatter and freeze both unchanged v5
reviews using the same source/context as the original comparison. Report all
scores, including failure. A pair of at least seven is the text-quality target;
this remains exploratory and does not pass prior failed controls or certify
audio, playback or production release.

Artifacts: `output/local-expression-rewrite-20260924/`.
