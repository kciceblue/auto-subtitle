# AM-S1: local Qwen audio-window attention repair

Selected acquisition under quality-attention-repair-plan-20260916.md. Mechanical diagnostic must pass before execution; its CPU synthetic outputs are not ASR quality evidence. One complete acquisition only, one model load, at most68 calls, zero retries. Root alone supplies production inputs. All artifacts remain local, no reviewer or Chinese writer is part of this command.

Retain the existing Qwen3-ASR1.7B weights, CUDAfloat16, actual defaultSDPA, automatic languageNone, empty context, one inference batch,512-token greedy normal-stop contract and exact saved PCM/gain. Add the pinned scoped adapter that passes the existing encoder block-diagonal mask to every audio attention layer; record actual audio/text backends before/after and all24 layer masks. Do not alter installed files, use a global patch, switch attention backend, extend windows or modify predecessor producer files.

First use the exact12-second silence and deterministic PCG64seed20260915 noise controls. A nonempty/malformed no-speech result retires before real audio. Then process all66 original18-25second windows, preserving every language/empty response and physical scope. Controls are unowned synthetic data. Low-level capture is explicitly only an I/O proof and is wrapped by the new treatment identity plus independent mask attestation.

Direct originals: output/quality-auto-source-20260915/native-plan.json, assets.json and original66 WAVs; shared original mono/gain/source provenance in output/quality-contextual-asr-projected-20260915; original controls from output/quality-contextual-asr-auto-20260915. Hash and compare direct inputs; no recursive historical generation/score prerequisite. Preserve exact raw output and parser result. Changed transcripts are not proof of accuracy or independent recognizer votes.

Inclusive local ceiling1200s with840work/360cleanup,60s per call. Only owned process may be stopped; restore previous backend on all exits, count cleanup and retain failure. Native-complete requires both controls pass, all66 real calls, matching tensor/parser replay, actual backend/mask proof and restored lifecycle. Result exposes source geometry, observation-envelope hash, code/artifact pins and native completion for local downstream use without scorer/history imports.

Only a successful acquisition permits AM-G1, under the parent plan: sameAU-G1 writer layout/sampler and unchangedB799, replacing its66 added automatic observations. New ASR readings and diagnostics stay local. A separate optional Astra final scalar uses only the unchanged previously authorized Japanese/context pool and generated Chinese, never audio/video or the new ASR text. No successful rerolls, source-based cherry-picking, automatic promotion or selected-output overwrite.

Pre-execution prerequisite passed: diagnostics/qwen-audio-attention-synthetic-20260916.json SHA2566bcf2f5c6147d5d85ec5147e0378378434010c53465b8aad1b28ce9e12b1ce33. Both actual SDPA/eager paths showed unmasked leakage4 versus masked0 and exact reference equality. Five adapter tests and five acquisition tests passed; root independently reproduced the tinySDPA result. These are synthetic correctness checks only.
