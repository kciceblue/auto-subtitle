# GT-G1: complete B evidence with bounded Gemma thinking

Selected 2026-09-16 after the automatic/forced draft block ended at scalar 2/2. This is one new local draft, not a regeneration of either AU result. Root owns dispatch. This document and controller registration fix the treatment before generation.

Use the exact B request from `output/quality-compact-raw-20260915/request.json`, fingerprint `e9815cde9b9e20523c9e0d92b643da945081fe4efd8db7ba6413e5578c8e2d29`. Preserve its instruction, body, native JSON schema, 66 original source owners, original context and all 799 raw observations/identity/scope links. No automatic/forced observations, old Chinese, model-written recap, diagnoses, human reference or score enters this writer. The direct B file and exact codec decode/encode roundtrip are authenticated; there is no transitive historical experiment prerequisite.

The sole changed generation treatment relative to LC-G1 is bounded thinking: selected Gemma4-31B QAT Q4 weights, context 163,840, full-SWA false, maximum output 16,384, temperature 1, top_p .95, top_k 64, repeat penalty 1, seed 20260913; `enable_thinking=true`, `reasoning_budget_tokens=4096`, and no `reasoning_effort` field. The same logical B request has a different native rendered template because thinking is enabled. Use the actual owned template and tokenizer for capacity before generation; complete prompt + 16,384 + 64 must fit. No trimming, partitions, temperature/seed search, second draft, retry, or HTTP fallback.

The native budget is per thinking block. It may include delimiter/UTF-8 overhead or re-arm for another block; it does not guarantee a 4,096-token aggregate thought or 12,288-token answer split. Total native generation is bounded by 16,384. Preserve raw SSE events, exact reasoning and answer separately, effective settings, complete native capacity and usage. Positive native reasoning is required for treatment adherence; a valid answer with no observed reasoning is retained and labelled `treatment_failed`, not a quality judgment. Invalid/truncated responses remain terminal failures. No local semantic checker or filter is added.

`src/local_gemma_native.py` owns one exact stream and offline replay. `scripts/gemma_thinking_screen.py` owns this fixed treatment, direct input pins, the local process lifecycle and optional review. The existing `profiles/long-context-gemma.json` supplies the owned deployment. All CPU preparation, input/hash replay, weight verification, load, prefill, generation, output replay and restoration count toward **3,600 local seconds: at most 3,180 work and 420 cleanup**. The stream's maximum duration is also bounded by remaining work time. Owned shutdown/restoration always runs, including after a deadline; overruns fail afterward rather than skip cleanup.

Local CLI, default folder `output/quality-gemma-thinking-20260916`:

- `--prepare`: pin only direct B request/source/context, local producer code and profile/server identity; save exact local/native requests. It does not read B registration, any baseline receipt or score catalog, and does not construct an evaluation bundle.
- `--execute-local`: one owned Gemma load/request; restore the previous backend; preserve parsed JSON and all exact 66 owner texts/timestamps. Require strict source/draft utterance-SRT roundtrip without normalization and native replay. Save immutable `completion.json` with direct artifact hashes and finish `screen.json` as `local_complete`. Any terminal attempt cannot be resumed or rerolled.
- `--score`: optional separate external step, already authorized. Only here load B's evaluation metadata, authenticate genuine baseline 2 and the unchanged 736-observation pool, and build the bundle from original source + new Chinese + original raw source + unchanged original context/evidence. No reasoning or new AU observations are exported. Replay local output and exact target again before dispatch. Reuse the earliest validated exact-target/pool primary via the existing duplicate catalog, including A/B/RT/LQ/R/full-draft/SS/LC/AU-G1/AU-G2 score metadata; an unresolved reservation blocks another attempt. At most one fresh primary.
- `--confirm`: optional only after the exact primary reaches 4; at most one fresh confirmation, bound to the same inputs and primary dispatch. Reuse any existing exact confirmation under the existing no-reroll guard. Confirmation remains a scalar benchmark result, not certified source fidelity, playback approval or automatic deployment.

External request/receipt time is reported separately as `runner_score_seconds`; local preparation/replay work remains in the local ledger. Primary and confirmation state live in their own review ledgers. A review outage, rejection or below-target score leaves `screen.json` locally complete and the immutable draft unchanged. No external service is needed to obtain the local files.

Synthetic checks cover actual shared SSE/native replay with all 799 invented B observations, no local evaluation/history access, exact input tampering, positive versus absent reasoning, terminal no-reroll, unconditional cleanup after budget exhaustion/startup failure, unchanged local output after external failure, earliest-primary reuse and one conditional confirmation. No test or development step consumes production semantics or runs a model.
