# AM: isolated Qwen-ASR attention boundary repair

Selected after the R/P block closed at3,3. Target remains consistent4; no candidate has reached it. All generation and diagnosis remain local. Astra is optional final scalar assessment only, never a production dependency or a repair source.

The installed Qwen audio encoder constructs cumulative window boundaries but omits its existing non-FA2 attention mask. The SDPA implementation ignores the cumulative lengths. This source-level finding matches open upstream PR103: https://github.com/QwenLM/Qwen3-ASR/pull/103. It does not prove historical runtime backend, transcript errors, or quality gains. The separate feasibility audit documents that limit.

## Fixed sequence and stop rules

1. A CPU synthetic diagnostic uses the actual installed audio-attention class, no weights/audio. Compare unmasked attention, the existing block-mask helper and an independent segmented reference. Verify cross-window isolation, uneven tails and backend identity. If the intended correction fails equivalence, stop this implementation direction before audio.
2. AM-S1: one new root-owned model load with a scoped adapter on the existing encoder. First run12-second silence and deterministic noise controls with automatic language; both must return native no-speech/empty text. Only then run all66 original short PCM windows. Fixed existing mono/gain, languageNone, empty context,512-token greedy decoding, no aligner, no cropping changes. Maximum68 calls, zero retries,1200s inclusive local budget (840work/360cleanup). Capture actual audio/text backends and layer masks/boundaries. If controls, runtime attestation, normal-stop proof or restoration fail, preserve the failure and stop without a Chinese candidate.
3. AM-G1: one local Gemma31 candidate. Use the AU-G1 request layout and unchanged799 base observations; replace only its66 added automatic-language readings with AM-S1 readings, with correct backend attribution. Keep source scope, all languages/empties, original context, original66 owner schema,163840 context,16384 total output, thinkingoff and the same sampler. Do not simultaneously compact the prompt or extend ASR windows. Maximum3600s local (3180work/420cleanup), zero retries/repair loops. Compare the final scalar with AU-G1=2; LC-G1/CS-G1=3 remain best experimental baselines.
4. Optional final assessment sees the already authorized unchanged736-observation Japanese/context pool and the frozen generated Chinese. New ASR readings, local reasoning and source diagnoses are never exported. At most one primary; an exact repeated target/pool reuses its earliest valid primary. Only a4+ primary permits the designated fresh confirmation. A confirmed single-file4 remains insufficient for a consistency claim.

No local stage can import/read review results or rely on Astra availability. Separate score sidecars cannot change local_complete or output bytes. Failed attempts and elapsed work remain saved. Do not overwrite site-packages, old producers, selected subtitles or sealed acquisition folders; use new versioned requests/receipts. Reused low-level tensor capture must be explicitly attributed and wrapped in the new treatment identity rather than presented as an old native treatment.

## Subsequent directions, not part of AM

If the bounded AM candidate is below4, do not reroll it. A separately declared120-second continuous Qwen-ASR acquisition tests wider acoustic/decoder context with corrected attention, controls and a larger output reserve. Another separately investigated option is one full-current-evidence local122B critic/recap followed by one Gemma repair; prior122B work did not establish this exact treatment. These are alternatives to more blind candidate rerolls. Their plans and time ceilings must be fixed before calls. No universal impossibility claim follows from a failed configuration.

Runtime reporting separates acquisition, generation, optional scoring and inherited cached evidence. Production adoption requires a fresh media-to-subtitle path and predeclared multiple fresh local runs with every output retained, followed by held-out media when available. Optional assessment does not serve as a repair guard.
